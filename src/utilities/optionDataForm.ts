// Define the shape of the option data to be exported for use
export interface OptionData {
  optionRoot: string;
  expirationDate: string;
  callPut: string;
  strikePrice: string;
  occSymbolCode: string;
}
